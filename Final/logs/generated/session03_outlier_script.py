# Outlier Detection Script — Session 03 (23/04/2026)
# Generated by AI (claude-sonnet-4-6) in response to data-quality prompt
# NOTE: This is the ORIGINAL AI-generated version before human editing
# Human fix: clamp fence to [0,10], use dropna(), output as markdown table

SCORE_COLS = ['toan', 'ngu_van', 'ngoai_ngu', 'vat_ly',
              'hoa_hoc', 'sinh_hoc', 'lich_su', 'dia_ly', 'gdcd']

for col in SCORE_COLS:
    Q1 = combined[col].quantile(0.25)
    Q3 = combined[col].quantile(0.75)
    IQR = Q3 - Q1
    lower = Q1 - 1.5 * IQR
    upper = Q3 + 1.5 * IQR
    out_count = ((combined[col] < lower) | (combined[col] > upper)).sum()
    print(f"{col}: Q1={Q1:.2f} Q3={Q3:.2f} IQR={IQR:.2f} "
          f"fence=[{lower:.2f},{upper:.2f}] outliers={out_count:,}")

# Issue noted by team:
# - lower fence can go negative (e.g., ngoai_ngu lower = -1.25) but scores can't be < 0
# - should clamp: lower = max(0.0, Q1 - 1.5*IQR), upper = min(10.0, Q3 + 1.5*IQR)
# - should use .dropna() before quantile to avoid NaN propagation
